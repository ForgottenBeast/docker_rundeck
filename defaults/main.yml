#shall we use let'sencrypt?
SSL: false
rundeck_domain_name: "rundeck.whatever.xyz"

rundeck_letsencrypt_domain_name: "rundeck.whatever.xyz"
rundeck_letsencrypt_mail: "none@throwaway.devnull"
#disabled when setting up a site, enable it when deploying to production
letsencrypt_testmode: "true"

rundeck_plugins_volumes: "rundeck_plugins"
